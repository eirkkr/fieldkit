# Style

- Use hyphens (`-`), not em dashes, in all project files.
- Markdown: wrap prose and code at the repo's configured line length (table rows
  exempt). The exact limit and any linter enforcement are per-repo.
